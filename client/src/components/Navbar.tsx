export default function Navbar() {
  return (
    <nav className="sticky top-0 z-20 flex items-center justify-between border-b border-gray-800 bg-bg/80 px-8 py-4 backdrop-blur">
      <span className="text-lg font-semibold text-accent">
        Openroot
      </span>
      <span className="text-sm text-gray-400">
        Price Intelligence Platform
      </span>
    </nav>
  );
}
